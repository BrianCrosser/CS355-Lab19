<% include header %>


<% if(rs.length > 0) { %>
<form method="post" action="/movie1/dropdown">
    <label for="movie1_id">Movies</label>
    <select id="movie1_id" name="movie1_id">
        <% for (var i=0; rs.length > i; i++) { %>
        <option value="<%= rs[i].MovieID %>">
            <%= rs[i].Title %>
        </option>
        <% } %>
    </select>
    <input type="submit">
</form>
<% } else { %>
The table does not contain any records.
<% } %>

<% include footer %>