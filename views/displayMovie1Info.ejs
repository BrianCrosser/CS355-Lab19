<% include header %>

<% if(rs.length > 0)
{ %>
    Title: <%= rs[0].Title %> <br />
    <br />
<%
    if (rs1.length > 0)
    {
        for(i = 0; i < rs1.length; i++)
        { %>
            Genre: <%= rs1[i].Genre %> <br />
        <% }

    }
}

else { %>
    No movie matched this Title id <%= movie1id %>.
<% } %>

<% include footer %>